var face = {
	onBegin: function (waitId, dataId) {
		var divW = $("#" + waitId);
		var divD = $("#" + dataId);

		divW.removeClass("hidden").addClass("show");
		divD.removeClass("show").addClass("hidden");
	}
};